<%= form_with(model: product, local: true) do |form| %>
<% if product.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(product.errors.count, "error") %>
      prohibited this product from being saved:</h2>

    <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<div class="col-md-12 text-center">
  <div class="field">
    <p>
      <label><%= form.label :name, :class => "register-form" %></label>
    </p>
    <%= form.text_field :name, id: :product_name ,:class => "form-control"%>
  </div>
</div>
<div class="col-md-12 text-center">
  <div class="field">
    <p><%= form.label :description %></p>
    <%= form.text_area :description, id: :product_description,:class => "form-control" %>
  </div>
</div>
<div class="col-md-12 text-center">
  <div class="field">
    <p><%= form.label :image_url %></p>
    <%= form.text_field :image_url, id: :product_image_url,:class => "form-control" %>
  </div>
</div>
<div class="col-md-12">
  <div class="actions">
    <%= form.submit 'Add', :class => "btn btn-success btn-block btn-round" %>
  </div>
<% end %>
</div>
